#include<stdio.h>
void main()
{
    int a,volume;
    printf("Enter the length of the side of the cube: ");
    scanf("%d",&a);
    volume=a*a*a;
    printf("Volume of the cube is: %d",volume);
    
}