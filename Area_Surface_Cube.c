#include<stdio.h>
void main()
{
    int a,area;
    printf("Enter the length of the side of the cube: ");   

    scanf("%d",&a);
    area=6*a*a;
    printf("Surface area of the cube is: %d",area);
}